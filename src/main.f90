program main

    use spherepack_wrapper_mod, only: &
        vector_t, &
        vector_ptr, &
        assignment(=), &
        operator(*), &
        grid_t, &
        sphere_t, &
        workspace_t

    ! Explicit typing only
    implicit none
    
    print '(A)', 'Successful build!'

end program main
